# SQLite by default (relative to prisma/schema.prisma → prisma/data/bayradar.db).
# For Vercel, use a Turso URL (libsql://… or https://…) plus TURSO_AUTH_TOKEN.
DATABASE_URL="file:./data/bayradar.db"
TURSO_AUTH_TOKEN=""

# Required in production. Used to encrypt/sign app secrets.
APP_SECRET="change-me"

# Optional. Protects /api/cron/poll except same-origin dashboard scans.
CRON_SECRET=""

# eBay Browse API (optional while the developer app is under review).
# Leave empty to auto-enable the mock catalog.
EBAY_APP_ID=""
EBAY_CERT_ID=""
EBAY_ENVIRONMENT="PRODUCTION"
EBAY_MARKETPLACE_ID="EBAY_DE"

# Force the simulated catalog: true | false
# Unset = Settings toggle, then auto-on when credentials are missing.
EBAY_MOCK_MODE=""

# OpenAI-compatible AI (optional). Settings UI overrides these when saved.
# Groq: https://api.groq.com/openai/v1  OpenRouter: https://openrouter.ai/api/v1
# Ollama: http://localhost:11434/v1 (no API key required)
AI_BASE_URL="https://api.openai.com/v1"
AI_API_KEY=""
AI_MODEL="gpt-4o-mini"

# Gixen sniping (optional). Settings UI overrides these when saved.
GIXEN_USERNAME=""
GIXEN_PASSWORD=""
GIXEN_ENABLED=""

# Telegram bot poller (optional). Settings UI is the primary source; these are fallbacks.
TELEGRAM_BOT_TOKEN=""
TELEGRAM_CHAT_ID=""

# Worker cadence when a monitor has no cron of its own.
WORKER_DEFAULT_CRON="*/5 * * * *"
POLLER_MODE="hybrid"
